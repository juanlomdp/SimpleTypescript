"use strict";
let employee2 = { id: 1,
    name: 'juan',
    retire: (date) => {
        return console.log(date);
    }
};
// controla que no haya 2 objetos con el mismo nommbre
// usar mejor TYPE alias
// employee.id=0; error
